import React from 'react';
import { Stack } from 'expo-router';

// Stack layout wrapping every screen in the /clinic flow with a default header style.
export default function ClinicLayout(): React.ReactElement {
  // stub: returns <Stack /> with screen options defined inline.
  return null as unknown as React.ReactElement;
}
