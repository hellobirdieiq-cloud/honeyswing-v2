# HoneySwing Architecture-Health Report — Phase 2 (2026-07-02, second execution, rev 2)

Scored against `architecture-health-rubric.md` (deduction model, 1000 points).
All measurements re-run live this session on the post-GoogleSignIn-removal
working tree (Clerk patch applied, `ios/` freshly regenerated by
`expo prebuild --clean`). Every deduction cites command output and/or
file:line. No axis was unmeasurable — network/auth-gated commands (supabase
type generation, madge/knip via npx, expo-doctor) all ran. Total is below the
850 threshold, so the rubric's extra adversarial pass was not triggered.

**Rev 2 (same day):** (1) clarified the `reliability.impact` persistence
surface and its Axis 5 treatment; (2) frozen baselines recorded in-report;
(3) **product decision incorporated: all existing historical swings will be
deleted and the product starts with fresh data.** Numeric scores are
measurements and are unchanged; the Top 20 ranking and the Final
Architectural Assessment are re-ranked around the new reality (no old rows to
preserve — backward compatibility with prior `analysis_version` rows is no
longer a top constraint; static/test coverage, calibration single-sourcing,
package boundaries, and capture-orchestration safety move above persisted-row
version compatibility, which is reframed as a define-it-fresh follow-up once
the new data shape stabilizes).

## 1. Executive summary

**Score: 795/1000.** Identical to the first Phase 2 pass — and that stability
is itself a finding: the intervening GoogleSignIn dependency removal and a
full `prebuild --clean` regeneration of `ios/` moved **zero** axes, confirming
the score is driven by durable structural properties, not day-to-day churn.

The analysis core is in excellent health: domain determinism is *proven*
(byte-identical double-run), capture-pipeline rate-independence is complete
(one decimation constant, ms-thresholds throughout, zero stale fps literals),
the persisted data contract is byte-fresh against the live schema, and the
native bridge survived a clean regeneration with zero drift. The losses
concentrate almost entirely in the *assurance* layers: the test gate covers
the pure math superbly but has four missing analysis-critical suites and zero
UI/glue tests (−52); the static gates see only part of the repo — the entire
lib test suite and the 28-file validation-script toolkit are type-checked by
nothing (−38); phase-detection calibration has literal tunable copies in the
coach-mode debug views and a prose-frozen legacy detector (−40); and the
dependency graph carries five dispatcher cycles plus one misclassified
runtime dependency (−40). The codebase remains easier to evolve than its
tooling can currently prove.

With the fresh-data decision, the risk ordering changes even though the
measurements don't: the historically scariest surface (rows written by past
pipeline versions) is about to be emptied, so the top priorities become the
assurance-layer gaps — gate perimeter, test placement, calibration sourcing,
and boundary codification — with the persisted-row contract re-entering as a
cheap, migration-free thing to define *correctly from day one* of the new
data shape.

## 2. Score table

| # | Axis | Max | Deductions | Score |
|---|---|--:|--:|--:|
| 1 | Layer & boundary integrity | 140 | −15 | **125** |
| 2 | Domain determinism & runtime purity | 70 | −0 | **70** |
| 3 | Capture-pipeline cohesion & rate-independence | 130 | −0 | **130** |
| 4 | Calibration single-sourcing | 90 | −40 | **50** |
| 5 | Data-contract integrity & evolution | 140 | −8 | **132** |
| 6 | Test health & gate integrity | 150 | −52 | **98** |
| 7 | Static-gate coverage | 90 | −38 | **52** |
| 8 | Dependency coupling & cycles | 90 | −40 | **50** |
| 9 | RN/Expo platform & native-bridge health | 60 | −10 | **50** |
| 10 | Dead code & change-amplification hotspots | 40 | −2 | **38** |
| | **TOTAL** | **1000** | **−205** | **795** |

## Frozen baselines (recorded in-report for future re-scores)

- **Final score: 795/1000** (2026-07-02, second execution; matches first execution).
- **Lint baseline: 36** problems (0 errors, 36 warnings) — matched this run.
- **God-file baseline: 9** non-test files > 600 lines: phaseDetectionFaceOn.ts
  1236, outbox.ts 998, Tab1LiveView.tsx 904, analysisPipeline.ts 814,
  settings.tsx 772, record.tsx 697, cue-block.tsx 695,
  phaseDetectionShared.ts 673, SwingSkeletonCanvas.tsx 605.
- **Orphan baseline (7, triaged from knip):** `components/GripHistoryRow.tsx`,
  `components/VisualCoachCard.tsx`, `lib/adapters/mediapipeHandAdapter.ts`,
  `lib/adapters/rtmHandAdapter.ts`,
  `packages/domain/clinic/metricComputation.ts`,
  `packages/domain/clinic/physicalLimitFlagEvaluator.ts`,
  `packages/domain/clinic/predictionAccuracyTracker.ts`.
- **Cycle baseline: 5** (madge, all intra `packages/domain/swing/`, all
  type-only back-edges).
- **Test baseline: 50 suites, 49 green**, 1 parked red (phaseDetectionDTL).

## 3. Detailed analysis per axis

### Axis 1 — Layer & boundary integrity: 125/140

**ATTACK FIRST.** The kill shot here would be a runtime import from
`packages/domain` into lib/app state (couples biomechanics to UI lifecycle,
untestable off-device), UI screens reaching into detector internals (freezes
the pipeline's internal shape), or platform imports inside `packages/`
(breaks tsx-runnability). Also probed: relocatability escapes and whether the
mixed import styles are hiding violations from single-style greps.

**Measured.**
- (a) Exactly one upward import in all of `packages/`:
  `packages/domain/swing/swingRowBuilders.ts:9` — `import type { Json } from
  '@/lib/database.types'` (verified type-only).
- (d) Zero react/react-native/expo imports under `packages/` (grep empty).
- (c) One packages-root escape: `packages/pose/rtmw/cocoWholebody.ts:1` →
  `../../../models/coco_wholebody_index.json`.
- (b) UI→`domain/swing` direct imports resolve to exactly three public
  product-logic modules: `tipFrequency` (4 files), `positiveReinforcement`
  (3), `captureValidity` (2). No detector or pipeline internals are imported
  by any app/components file.

**Deductions.** −10: the type-only upward import is enumerated in no written
in-repo allowlist. −5: the models-JSON escape is likewise un-allowlisted (the
pose package is silently non-relocatable).

### Axis 2 — Domain determinism & runtime purity: 70/70

**ATTACK FIRST.** The kill shot: a clock or randomness read feeding a scored,
phase, or persisted analysis value — it would rot every replay baseline in
`scripts/` without a single test failing, and destroy the byte-identical
refactor-proof technique. Second-order: console noise burying failures under
an exit-code-only runner.

**Measured.**
- (a) 9 ambient-read hits in non-test domain code: `tipFrequency.ts:223,318,
  346,385,492` (`Date.now()` session-window state),
  `positiveReinforcement.ts:300` (`Math.random()` message pick),
  `personalBandCalculator.ts:44,68,94` (`updatedAt` stamps). Zero `console.*`.
- (b) Reachability: `analysisPipeline.ts` imports none of the three modules
  (grep empty) — nothing is on the `analyzePoseSequence` path; none feeds a
  scored/phase/persisted analysis value.
- (c) Determinism double-run: `npm test -- scoring` twice → diff empty
  (**DETERMINISM: IDENTICAL**).

**Deductions.** None per schedule. (The `tipFrequency` internal clock state
is a testability wart — logged as finding #16, not a schedule deduction.)

### Axis 3 — Capture-pipeline cohesion & rate-independence: 130/130

**ATTACK FIRST.** The kill shot: a raw frame-count window sized for one fps
surviving the 120fps flip — it would silently halve a search window
on-device. Also probed: duplicated decimation constants and undocumented
cross-file couplings (timeout↔decimation).

**Measured.**
- (a) `ANALYZER_DECIMATION` defined once (`lib/cameraFormat.ts:11`), one
  importer (`captureProcessing.ts:41`), passed down from there. No copies
  anywhere in app/lib/packages/scripts.
- (b) Frame-literal sweep across the five capture-chain files: only the
  source-of-truth format definition (`CAPTURE_FPS = 240`,
  `cameraFormat.ts:10`) and comments.
- (d) `msToFrames`/`msPerFrame` consumed in 11 non-test domain files; shared
  tables carry ms-siblings for every frame constant.
- Coupling: `EXTRACTION_TIMEOUT_MS` (`captureProcessing.ts:153`) carries an
  explicit comment tying it to decimation 2 and flagging on-device
  verification status.

**Deductions.** None.

### Axis 4 — Calibration single-sourcing: 50/90

**ATTACK FIRST.** The kill shot: the Dave-clinic recalibration edits
`EXTERNAL_ASSUMPTIONS` and a copy somewhere keeps running old rules — worst
if the copy is in the very coach-mode screens used to *do* the tuning, which
would have the coach calibrating against stale thresholds with no error
anywhere.

**Measured.**
- (a) Literal copies of shared tunables in `app/clinic/coach-mode/
  signalCompute.ts:144` (`MIN_TRAVEL = 0.04` = `dtl.top.minTravel`) and
  `:423` (`VEL_NOISE_FLOOR = 0.008` = `dtl.finish.velocityFloor`); line 386
  additionally hardcodes the string `'MIN_TRAVEL=0.04'` into displayed rule
  text. Display-only surfaces; no comment names the shared source.
- (b) Detector-private tunables: `phaseDetectionLegacy.ts:107,108,136,165,166`
  (five constants, four value-identical to the shared `dtl` block) and
  `phaseDetectionFaceOn.ts:211–213` (three impact tunables).
- (c) One shared `DetectedPhase` contract across all four phaseDetection
  files — no divergence.

**Deductions.** −20: two uncommented literal copies in a display surface
(−10 each). −20 (cap): ≥8 detector-private tunables. Classification note
(unchanged from first pass): legacy's value-identical constants score under
the private-tunable cap, not as decision-path copies (−30 each), because
ARCHITECTURE_MAP documents the legacy detector as intentionally frozen 1:1 —
a stated design decision. Undocumented, this axis would be ≤10.

### Axis 5 — Data-contract integrity & evolution: 132/140

**ATTACK FIRST.** The kill shots, in order of irreversibility: a
non-idempotent read-time identity correction double-swapping legs in history
playback; a naive `new Date()` on an offset-less DB string shifting instants;
schema/type drift letting the compiler bless invalid writes; old
`analysis_version` rows becoming unrenderable; write-only fields rotting
unnoticed; duplicate write paths double-inserting.

**Measured.**
- (a) Column census (files referencing, excluding database.types):
  `swing_debug` 10, `pose_full` 6, `metric_confidences` 5, `trail_points` 5,
  `angles` 11, `tempo` 22, `phases` 11 — all consumed. `category_scores` 3:
  write (`persistSwing.ts:134`), select + row type (`swingStore.ts:64,115`),
  test fixture — **value consumed nowhere**, no external-consumer note at the
  write site.
- (b) Idempotency **is tested**: `lowerBodyIdentity.test.ts:216` ("#9
  idempotency — second application is a no-op").
- (c) One naive-Date suspect: `lib/swingLimit.ts:60`
  `new Date(user.created_at)` — source is the Supabase **auth** user
  (`getUser()`), whose `created_at` is ISO-8601 with timezone; not the
  offset-less DB-column class. All other hits serialize *from* epoch ms.
- (d) `npx supabase gen types typescript --linked` vs
  `lib/database.types.ts`: **0-line diff** (byte-identical).
- (e) Both `swings` inserts live in `lib/persistSwing.ts:177,192` (primary +
  FK-heal retry) — one orchestration path.
- (f) `analysis_version` written and selected; no read-path branches on it;
  tolerance exists structurally (nullable analysis columns +
  `reconstructAnalysis.ts`, unit-tested).

**Clarification — `reliability.impact` (finding #15) vs this axis's
deduction.** `reliability.impact` is **not a DB column** (zero matches in
`lib/database.types.ts`). It is a sub-field of the detectors' rule-debug
tree, written at `packages/domain/swing/phaseDetectionFaceOn.ts:909`
(`reliability.impact = selection.impactReliability`) and
`packages/domain/swing/phaseDetectionDTL.ts:412`
(`reliability.impact = "high"`), and it reaches the database only *inside*
the `swing_debug` JSON column (via the detectors' `PhaseRuleDebug` →
`swing_debug.phase_rules`). It is therefore **not a second write-only column
the schedule missed**: the Axis 5 schedule item operates at persisted-column
granularity, the `swing_debug` column has 10 in-repo referencing files and a
documented diagnostic purpose ("audit trail; nothing here feeds the score" —
ARCHITECTURE_MAP), and the sub-field's semantics are documented at the write
site (`phaseDetectionFaceOn.ts:428` — every fallback carries
`reliability.impact = "low"`). **Corrected Axis 5 deduction: unchanged at −8
(category_scores only); score stands at 132/140.** `reliability.impact`
remains listed in the Top 20 as a *risk note* — a pre-staged, in-app-unread
signal that must be validated before any consumer is attached — not as a
scoring miss.

**Deductions.** −8: one persisted write-only field (`category_scores`)
undocumented at its write site.

**Interpretation update (fresh-data decision).** The measurements above
stand, but the *risk weight* of items (f) and old-row tolerance drops: with
all historical swings being deleted, there are no prior `analysis_version`
rows to preserve. The actionable reading flips from "protect old rows" to
"define the contract explicitly while the table is empty — the cheapest
moment there will ever be."

### Axis 6 — Test health & gate integrity: 98/150

**ATTACK FIRST.** The kill shot is a gate that lies: discovery quietly
skipping suites or exit codes being swallowed — this repo's own history
(the pre-773cabd runner) proves the failure class is real here. Second-order:
a permanently red suite normalizing red output, and test mass sitting where
tests were easy rather than where regressions are expensive.

**Measured.**
- (a) `npm test`: **50 files, 49 passed, 1 failed** —
  `phaseDetectionDTL.test.ts`, the documented parked red.
- (b) 50 `*.test.ts` on disk under lib/+packages/ == 50 executed. Zero
  `*.test.tsx`/`*.spec.ts` files exist; the runner (`run-tests.mjs:30`)
  matches only `.test.ts` — a component test would be silently invisible
  (design blind spot, zero affected files today).
- (c) Exit propagation intact: `run-tests.mjs:55`
  `process.exit(failed.length ? 1 : 0)`.
- (e) Sibling gaps among the 10 analysis-critical modules: **4** —
  `phaseDetectionLegacy`, `phaseDetectionShared`, `tempoAnalysis`, `angles`.
- (d) Distribution: 30 domain/swing, 17 lib, 2 clinic, 1 pose; **0 in app/
  and components/** — while documented capture invariants (refs-as-refs,
  outbox mutual exclusion) live in that code, and
  `components/SwingSkeletonCanvas.tsx` holds 605 lines of coordinate math.

**Deductions.** −5 documented parked red; −5 runner-invisible test class (by
design, zero files); −32 four missing analysis-critical siblings (−8 each);
−10 zero app/components tests holding documented invariants.

### Axis 7 — Static-gate coverage: 52/90

**ATTACK FIRST.** The kill shot: "tsc passes" meaning nothing for the file
you just changed — unchecked islands in shipped trees, tests executing under
type-stripping tsx with no typecheck anywhere, and the validation-script
toolkit silently breaking on domain refactors.

**Measured.**
- (a) `npx tsc --noEmit`: exit 0.
- (b) Census (loaded vs on-disk non-test): app 47/47; **lib 62/64**;
  **components 9/11**; packages 84 (incl. tests); **scripts 0/28**. The four
  unchecked files: `components/GripHistoryRow.tsx`,
  `components/VisualCoachCard.tsx`, `lib/adapters/mediapipeHandAdapter.ts`,
  `lib/adapters/rtmHandAdapter.ts` (unchecked because orphaned — see Axis 10;
  the perimeter defect is that reachability-based inclusion makes *any*
  orphan invisible).
- (c) 33 test files loaded = exactly the packages/ suites; **all 17 lib test
  suites** (including `outbox.test.ts`, `persistSwing.canary.test.ts`,
  `swingStore.test.ts` — the persistence seam) are outside every tsconfig,
  type-checked by nothing.
- (d) Lint: 36 problems (0 errors, 36 warnings) — exactly the recorded
  baseline; no deduction.

**Deductions.** −8 four unchecked shipped files (−2 each); −20 a whole test
class outside every tsconfig; −10 scripts/ imports production domain code
unchecked. (No double-count: Axis 10 scores the orphans' existence; this axis
scores the gate perimeter that lets orphans go unchecked.)

### Axis 8 — Dependency coupling & cycles: 50/90

**ATTACK FIRST.** The kill shot: an import cycle through the capture-path
stores or event bus — undefined-at-init under Metro load order, breaking on
an unrelated import addition. Second-order: duplicate worklet runtimes
desyncing on upgrade, and dep-class misclassification poisoning every tool
that trusts package.json.

**Measured.**
- (a) madge@8: **5 cycles**, all inside `packages/domain/swing/`:
  dispatcher↔DTL, dispatcher↔FaceOn, dispatcher↔Legacy,
  dispatcher→cameraAngle→phaseDetectionShared→dispatcher, and
  watchImu↔clockAlign. Import inspection shows every back-edge is
  `import type` (`phaseDetectionLegacy.ts:9`, `phaseDetectionDTL.ts:18`,
  `phaseDetectionFaceOn.ts:11`, `phaseDetectionShared.ts:18`,
  `clockAlign.ts:19`) — type-erased, so the runtime init-order attack cannot
  fire; the extraction coupling remains.
- (b) `npm ls`: `react-native-worklets@0.8.3` required by
  `react-native-reanimated@4.1.7`; `react-native-worklets-core@1.6.3`
  required by `react-native-vision-camera@4.7.3` — duality is
  constraint-driven; no deduction.
- (c) One runtime import of a devDependency: `@supabase/supabase-js`
  (devDependencies) imported by `lib/persistSwing.ts` / `lib/supabase.ts`.
  (`patch-package`, newly added, is a genuine install-time devDependency —
  correctly classified.)

**Deductions.** −30 (cap) five intra-layer cycles, schedule applied
literally with the type-only mitigation recorded; −10 one runtime devDep
import.

### Axis 9 — RN/Expo platform & native-bridge health: 50/60

**ATTACK FIRST.** The kill shot: the built app running different frame-
processor code than the reviewed source (drift between `native-assets/ios/`
and the `ios/honeyswing/` build copies), or new-arch flags disagreeing across
surfaces. This axis got a live stress test this session: `ios/` was fully
regenerated by `expo prebuild --clean` hours before measurement.

**Measured.**
- (a) Source-vs-copy drift across all mirrored Swift files: **zero** — and
  this is now proven *post-regeneration*, the strongest possible evidence
  that the copy automation works.
- (b) Copy mechanism automated: `plugins/withHoneyNative.js:63`.
- (c) `newArchEnabled` agrees across `app.json:10`,
  `ios/Podfile.properties.json:5`, `android/gradle.properties:38`.
- (d) expo-doctor: **2 checks failed**, 9 packages out of date vs SDK 54
  (expo-font, expo-router, expo-updates among them); no waiver documented
  in-repo.

**Deductions.** −10: two undocumented failed doctor checks (−5 each).

### Axis 10 — Dead code & change-amplification hotspots: 38/40

**ATTACK FIRST.** The kill shot: dead-code tooling deleting intentionally
parked code (a product decision made by a linter), or previously decomposed
screens silently regrowing.

**Measured.**
- (a) knip@5: 96 "unused files"; triaged against the parked-code allowlist
  built beforehand (vanished result-screen feature effects; inert Watch-IMU
  modules; parked DTL suite). ~50 are the custom runner's test files and ~30
  are scripts/ + `plugins/withHoneyNative.js` — knip false positives (no
  entry config for the custom runner, npx-tsx scripts, or config plugins).
  Genuine orphans: **7** (list in Frozen baselines above). None on the
  capture/persist path.
- (b) God-file census (>600 lines, non-test): **9 files** (list in Frozen
  baselines above). Equals the recorded baseline (9) — trajectory flat, no
  deduction.

**Deductions.** −2: seven off-path orphans (−1 each beyond the first five).

## 4. Top 20 architecture findings, ranked by long-term impact

*Re-ranked for the fresh-data reality (historical swings will be deleted):
assurance-layer gaps lead; persisted-row version compatibility drops but is
retained as a define-it-fresh follow-up.*

1. **All 17 lib test suites are type-checked by nothing** (tsc loads 33 test
   files, all packages/) — the persistence seam's tests (outbox, persistSwing
   canary, swingStore) run type-stripped and can assert against stale APIs
   while passing.
2. **tsconfig include = app+packages only** — lib/ and components/ are
   checked solely via import-reachability, so any unimported file (today: 4)
   is invisible to the gate; the perimeter is defined by luck of the import
   graph, not by the source tree.
3. **scripts/ (28 files) imports production domain code with zero
   typechecking** — every domain refactor can silently break the entire
   ground-truth validation toolkit the calibration workflow depends on.
4. **Four analysis-critical modules lack sibling suites**
   (phaseDetectionLegacy, phaseDetectionShared, tempoAnalysis, angles) — the
   shared threshold table and the tempo/angle math that feed the headline
   score have no dedicated executable guard.
5. **Zero tests over app/ and components/** while documented capture
   invariants (refs-as-refs, outbox mutual exclusion) and 605 lines of
   coordinate math (SwingSkeletonCanvas) live there — institutional memory is
   the only regression barrier, and with fresh data every newly captured
   swing depends on this unguarded orchestration path.
6. **Coach-mode debug views hold literal copies of shared tunables**
   (`signalCompute.ts:144,423` = `dtl.top.minTravel`,
   `dtl.finish.velocityFloor`) — recalibration desynchronizes the very
   screens used for tuning.
7. **The legacy detector's freeze is prose, not contract** — five private
   constants (`phaseDetectionLegacy.ts:107–166`), four value-identical to the
   shared table; nothing enforces or verifies the documented "1:1 frozen"
   status. (Fresh data also reopens the question of whether the frozen
   legacy behavior still needs preserving at all.)
8. **The test runner discovers only `.test.ts`** (`run-tests.mjs:30`) — the
   first component/`.tsx` test ever written will silently never run; the gate
   has a pre-installed blind spot.
9. **Boundary exceptions are un-allowlisted and the domain has no declared
   public API** — the sanctioned type import (`swingRowBuilders.ts:9`) and
   the models-JSON escape (`cocoWholebody.ts:1`) exist only as convention;
   `packages/` is silently non-relocatable and the pure-core property is
   guarded by discipline, not mechanism.
10. **Five import cycles through the phase dispatcher** (madge; type-only
    back-edges) — no runtime risk, but the detectors and dispatcher are
    mutually locked, which taxes any future extraction of the domain package.
11. **A permanently red suite (phaseDetectionDTL) normalizes red gate
    output** — a second unrelated red in the same tail would look identical
    at a glance.
12. **`@supabase/supabase-js` is a devDependency runtime-imported by the
    persistence layer** — every tool that trusts dependency classes (EAS,
    knip, a future workspace split) will misjudge the app's true runtime
    graph.
13. **The persisted swing row still has no explicit versioned contract** —
    `analysis_version` is written but nothing keys on it. *Re-ranked down
    from #1:* with all historical rows being deleted, there is no legacy
    compatibility burden — which makes this the cheapest it will ever be to
    define the contract explicitly (field registry, owners, consumers,
    per-version read guarantees) as the new clean data shape stabilizes.
    Deferred, not dismissed: the moment fresh rows accumulate, this climbs
    back toward the top.
14. **`category_scores` is persisted write-only, undocumented at the write
    site** (`persistSwing.ts:134`) — fresh data makes this a now-or-never
    decision point: either name its consumer at the write site or stop
    writing it before the new corpus accumulates it.
15. **`reliability.impact` is written with zero in-app consumers** — now
    clarified: not a DB column but a sub-field inside the `swing_debug` JSON
    column, written at `phaseDetectionFaceOn.ts:909` and
    `phaseDetectionDTL.ts:412` (see Axis 5 clarification; no scoring change).
    Remains a pre-staged, unvalidated input that must not gain a consumer
    before the xCross impact validation completes.
16. **`tipFrequency` is a stateful, clock-coupled domain class** (5×
    `Date.now()`, private session state) inside the "pure" package — off the
    analyze path today, but it is the pattern most likely to erode the
    determinism guarantee as it grows.
17. **Seven orphaned modules** (2 components incl. VisualCoachCard — likely a
    vanished-features casualty, 2 hand adapters, 3 clinic domain modules) —
    audit and upgrade tax; VisualCoachCard needs a product decision, not
    deletion.
18. **Nine files exceed 600 lines**, led by the scoring-critical
    `phaseDetectionFaceOn.ts` (1236) and `outbox.ts` (998) — flat trajectory
    today, but these are where unrelated changes will collide first.
19. **expo-doctor fails 2 checks (9 packages behind SDK 54) with no waiver
    record** — routine SDK upgrades start from an undocumented deficit on a
    bleeding-edge RN 0.81 + React 19 + new-arch stack.
20. **Vanished-feature effects still execute in result.tsx**
    (gripCloud/limitHit/coachName/positiveResult; parked product decision) —
    render cost and throw surface for UI that no longer exists, plus a
    standing trap for naive dead-code cleanup.

## 5. Final architecture score

**795 / 1000** (re-measured; unchanged from the first Phase 2 pass — the
GoogleSignIn removal and full ios/ regeneration moved no axis, and the
zero-drift result after `prebuild --clean` upgraded Axis 9's evidence from
"currently in sync" to "proven regenerable"). The fresh-data product decision
changes priority interpretation only; it does not alter any measurement or
deduction.

## 6. FINAL ARCHITECTURAL ASSESSMENT

*One week as lead architect, architecture only, ranked by long-term
evolvability impact — re-ranked for the fresh-data reality (no historical
swings to preserve). Structural decisions, not code.*

**#1 — Close the static-analysis perimeter to the whole repository.**
Strict TypeScript is this repo's refactor amplifier, and it currently sees
neither the lib test suite, nor scripts/, nor any unimported file. Every
blind spot is a place where "it compiles" is false comfort — including the
persistence tests that will guard the *new* data shape. With old rows gone,
the codebase is about to change faster, not slower; the perimeter must be
complete before that churn starts. Extending it until every source and test
file is loaded by some typecheck config is the highest multiplier available:
it strengthens every future change in every subsystem at zero design risk.
Findings #1, #2, #3.

**#2 — Reallocate test mass to the risk surface and fix the gate's blind
spots.** Test placement mirrors ease, not regression cost: superb coverage of
pure math beside zero coverage of the capture orchestration, coordinate
mapping, and four analysis-critical modules — and the runner cannot even
discover a `.tsx` test yet. Under fresh data this gap is sharper: every swing
in the new corpus will be produced by exactly the unguarded capture path, so
a capture regression now poisons the *only* data there is. Policy, not
sprint: every documented invariant gets an executable guard; the runner
discovers every test-file class before the first such file exists. Findings
#4, #5, #8, #11.

**#3 — Finish calibration single-sourcing and make the legacy freeze a
decision.** Recalibration against real coaching sessions is the product's
core iteration loop, and `EXTERNAL_ASSUMPTIONS` exists precisely to make it a
single edit. That property fails at both edges today: the coach-mode tuning
screens carry uncommented copies, and the legacy detector's freeze is
enforced by nothing. Fresh data strengthens the case — the freeze existed to
keep old unknown-angle swings scoring identically, and with those rows gone
the freeze itself is up for re-decision. Complete by-reference consumption;
declare, guard, or retire the legacy freeze. Findings #6, #7.

**#4 — Declare the domain packages' public API and codify boundary rules.**
The layering is healthy today — by discipline, not mechanism. No allowlist
for the two known escapes, no declared surface separating "product logic UI
may import" from "pipeline internals," mixed import styles that defeat
auditing, and type-cycles binding the dispatcher to its detectors. Writing
the public API down and making the boundary machine-checkable makes the
repo's single most valuable property — the pure, replayable analysis core —
durable under team growth, and unlocks the extraction the `packages/` layout
has always implied. Findings #9, #10, #12, #16.

**#5 — Define the fresh persisted-swing contract from day one.**
Demoted from #1 by the fresh-data decision — and transformed by it: this is
now an *opportunity*, not a debt. The table will be empty; there will never
be a cheaper moment to make the row contract explicit — field registry with
owners and consumers (deciding `category_scores`' fate before the new corpus
accumulates it), real `analysis_version` read semantics, and a validation
gate before pre-staged signals like `reliability.impact` gain consumers. Do
it as the new data shape stabilizes, not after rows pile up — because the
moment they do, this item starts climbing back toward #1. Findings #13, #14,
#15.

## 7. Follow-ups — accepted test-coverage gaps (2026-07-02, safety-net project)

The "strengthen the engineering safety net" project (findings #1–#5, #8;
commits 6f66c6a…b8454e9) closed the typecheck perimeter, fixed runner
discovery, and added suites for tempoAnalysis / angles / phaseDetectionShared
/ skeletonProjection (test baseline now 54 suites / 53 green / 1 parked red).
The following surfaces were **deliberately left untested** so the gap is a
decision, not an accident. Rationale everywhere: no fake confidence from
heavy React-Native mocks; device validation remains the real gate for
native-bound flows.

- **`lib/useSwingCapture.ts` (refs-as-refs invariant)** — hook with top-level
  react-native / vision-camera / expo imports; not node-loadable. Its
  documented invariants stay guarded by device QA; its extracted decision
  logic is already covered by `packages/domain/swing/captureFlow.test.ts`.
- **`lib/captureProcessing.ts` `processRecordedVideo`** — native-bound glue
  (vision-camera-pose, pose extraction, persistence side-effects). All
  branching it performs is delegated to the tested pure `captureFlow`
  helpers; a node test would mock away everything it actually does.
- **`app/analysis/useSwingSource.ts` / `useSwingVideoClock.ts`** — React
  hooks; exercising them off-device requires an RN test-renderer harness,
  rejected by the no-heavy-mocks principle. Revisit only if a pure decision
  core is ever extracted from them.
- **Deferred: on-device visual check of the skeleton overlay** — the
  Step 4.1 extraction (commit 9d42c60) is pure code motion and is pinned by
  `components/skeletonProjection.test.ts`, but the overlay has NOT been
  re-verified on device (operator decision). Verify both render modes
  (driven video-overlay identity mapping and self-clocked hip-anchored fit)
  before the next device build.
- **Still blocked, not a gap decision:** `phaseDetectionLegacy` tests wait on
  the legacy-freeze decision (finding #7); the parked `phaseDetectionDTL`
  red remains out of scope.

---
*Measurement provenance: all commands executed 2026-07-02 (second Phase 2
run) from the repo root; first background batch invalidated by a stale
working directory and re-run. Baselines used: lint 36 (matched), god-files 9
(matched); full frozen-baseline set recorded above. Rev 2 additions:
reliability.impact clarification (Axis 5, no scoring change), in-report
baselines, fresh-data re-ranking. No production code was modified during
scoring or revision.*

---

## Rev 3 — post-coach-pivot delta (2026-07-04)

**The 795/1000 score above predates the coach pivot and is no longer the
number for this tree.** Between rev 2 and this delta, four coach-pivot
commits landed on main (`5c720c2..b758584`, net +1,099/−7,384 lines: clinic
removed entirely, coach view + `player_profiles` sync + attribution RLS
added). This section records what was re-verified against HEAD `b758584` on
2026-07-04 — it corrects the frozen baselines and the findings ledger
without rewriting the axis scores, which remain honest 2026-07-02
measurements.

### Frozen baselines — corrected (4 of 6 were stale)

| Baseline | Rev 2 (2026-07-02) | Measured 2026-07-04 |
|---|---|---|
| Lint | 36 warnings | **10** (0 errors, 10 warnings) — re-run live |
| God-files (>600, non-test) | 9 | **6** — Tab1LiveView.tsx (904) and cue-block.tsx (695) deleted with clinic; SwingSkeletonCanvas.tsx decomposed 605→538. Remaining: phaseDetectionFaceOn.ts 1236, outbox.ts 998, settings.tsx **836** (grew from 772), analysisPipeline.ts 814, record.tsx 698, phaseDetectionShared.ts 673 |
| Orphans | 7 | **4** — the 3 `packages/domain/clinic/*` orphans deleted with clinic; GripHistoryRow.tsx, VisualCoachCard.tsx, and the 2 hand adapters remain |
| Cycles | 5 | **7** — madge@8 re-run live. The 5 domain cycles are unchanged (dispatcher↔DTL/FaceOn/Legacy, dispatcher→cameraAngle→shared→dispatcher, watchImu↔clockAlign). **2 NEW cycles in lib/, introduced by the coach pivot:** `playerProfiles → playerProfilesSync → playerProfiles` and `playerProfiles → playerProfilesSync → playerProfilesSyncCore → playerProfiles`. Both back-edges are `import type` (`playerProfilesSync.ts:12`, `playerProfilesSyncCore.ts:12`); the runtime edge is `playerProfiles.ts:5`. Same type-erased mitigation class as the domain 5 — no runtime init-order risk — but the first cycles outside `packages/domain/swing` and outside Axis 8's rev 2 census |
| Tests | 50 suites / 49 green (§7: 54/53/1) | **54 / 53 / 1** — re-run live. Composition changed: −2 clinic suites (cueEfficacyScorer, personalBandCalculator), +2 coach suites (lib/coachData.test.ts, lib/playerProfilesSync.test.ts). Only red = parked phaseDetectionDTL |

### Doc defect in rev 2 (recorded as a finding in its own right)

- **Rev 2 is internally inconsistent: §7 vs the Axis 6/7 score tables.**
  §7 records that the safety-net project (commits 6f66c6a…b8454e9) closed the
  typecheck perimeter, fixed runner discovery, and moved the test baseline to
  54/53/1 — yet the Axis 7 analysis still deducts −38 for the open perimeter,
  Axis 6 still deducts for the missing sibling suites and the `.tsx`
  blind spot, and the frozen-baselines block still records 50/49 tests. The
  score table and §7 describe two different repo states. Any Phase 3 re-score
  must re-measure axes 6 and 7 from scratch rather than carrying rev 2's
  deductions forward.

### Findings ledger — resolved since rev 2

- **#1/#2/#3 (typecheck perimeter; Axis 7 −38): CLOSED.** `tsconfig.node.json`
  now loads `lib/**/*.test.ts` + `scripts/**/*.ts`; `typecheck` /
  `typecheck:node` / `typecheck:all` npm scripts exist; both configs exit 0.
- **#4 (missing sibling suites; part of Axis 6 −32): 3 of 4 closed** —
  tempoAnalysis, angles, phaseDetectionShared suites exist. Only
  phaseDetectionLegacy remains, deliberately blocked on the freeze decision
  (finding #7).
- **#6 (calibration copies in coach-mode; Axis 4 −20): RESOLVED BY
  DELETION** — `app/clinic/coach-mode/signalCompute.ts` removed with the
  pivot. `app/clinic/` now holds only `_layout.tsx` + `imu-debug.tsx` (kept
  pending the watch-IMU decision).
- **#8 (runner discovers only `.test.ts`): FIXED** — `run-tests.mjs:31`
  matches `.test.ts` and `.test.tsx`.

### Findings ledger — still true (re-verified, current file:line)

- **#7** legacy freeze still prose-only — `phaseDetectionLegacy.ts:107-108`
  constants present; faceOn impact tunables at `phaseDetectionFaceOn.ts:211-213`.
- **#9** single upward type-only import `swingRowBuilders.ts:9` + models-JSON
  escape `cocoWholebody.ts:1`; still no allowlist; zero react/react-native/
  expo imports under `packages/`.
- **#12** `@supabase/supabase-js` still in devDependencies, runtime-imported
  by `lib/persistSwing.ts` / `lib/supabase.ts`.
- **#14** `category_scores` still write-only — written at
  `persistSwing.ts:127`, zero value consumers.
- **#15** `reliability.impact` still has zero consumers — written at
  `phaseDetectionFaceOn.ts:909` and `phaseDetectionDTL.ts:412`.
- **#16** tipFrequency still stateful/clock-coupled (`Date.now()` at
  223, 318, 346, 385, 492); `positiveReinforcement.ts` `Math.random()` moved
  to :299. personalBandCalculator's 3 ambient hits are GONE (file deleted
  with clinic) — Axis 2's non-test ambient-read census shrinks accordingly.
- **#20** vanished-feature effects still execute in `app/analysis/result.tsx`
  but are now gated behind `isOwnSwing` (`result.tsx:86,257,283,306`);
  gripCloud moved to `useSwingSource.ts:48,141`.
- **#19** expo-doctor: **NOT RE-MEASURED** (network-gated command, not run
  this pass); rev 2's "2 failed checks / 9 packages behind" status is
  unconfirmed either way.

### Stale claims corrected

- **Axis 1(d)** — "UI imports exactly three domain modules" is no longer
  true. Exact census (re-grepped 2026-07-04, non-test app/ + components/):
  **9 files import 10 distinct `packages/domain/swing` modules.**

  | Importer | Modules |
  |---|---|
  | `app/_layout.tsx:14-15` | tipFrequency, positiveReinforcement |
  | `app/onboarding.tsx:19` | tipFrequency |
  | `app/analysis/result.tsx:13,18,19,22,23,26` | phaseDetection (type-only), tipFrequency, confidenceScore, positiveReinforcement, tempoDisplay |
  | `app/analysis/useSwingSource.ts:23,24-27,29` | captureValidity, analysisPipeline, tempoDisplay |
  | `app/coach/index.tsx:25` (NEW, coach pivot) | scoring |
  | `components/VisualCoachCard.tsx:5-10` | angles (type-only), captureValidity, scoring, tipFrequency, metricDefinitions |
  | `components/SwingHistoryList.tsx:6-9` | scoring |
  | `components/SwingArtCard.tsx:5` | phaseDetection (type-only) |
  | `components/SwingSkeletonCanvas.tsx:5` | phaseDetection (type-only) |

  Distinct modules: tipFrequency, positiveReinforcement, captureValidity,
  tempoDisplay, phaseDetection, confidenceScore, analysisPipeline, scoring,
  angles, metricDefinitions. Still zero imports of detector internals
  (phaseDetectionFaceOn/DTL/Legacy/Shared) — but rev 2's "exactly three
  public product-logic modules" framing is dead.

### Unscored surfaces — scope any Phase 3 re-score must add

The coach pivot added ~1,100 lines the rubric has never seen:

- **Coach view**: `app/coach/index.tsx` (307 lines, roster + feed),
  `app/coach/_layout.tsx`.
- **Player-profile sync**: `lib/playerProfilesSync.ts` + pure
  `lib/playerProfilesSyncCore.ts` (+ suite), `lib/playerProfiles.ts`.
- **Coach data / attribution**: `lib/coachData.ts` + pure
  `lib/coachDataCore.ts` (+ suite).
- **Data contract**: `player_profiles` table (`database.types.ts:168`),
  `swings.player_profile_id` attribution column, attribution/storage RLS
  migration (+70 SQL). Axis 5's column census and RLS reasoning do not cover
  any of this.
- **Domain**: `packages/domain/swing/phaseTags.ts` (relocated shared types;
  internal-only importer).
- **Dependency graph**: the 2 new lib/ cycles recorded in the baselines
  table above — Axis 8's rev 2 claim "all cycles inside
  packages/domain/swing" no longer holds.

*Rev 3 provenance: lint, tsc (both configs), the full test suite, madge@8
(cycles), and the UI→domain import census were re-run/re-measured live
2026-07-04; god-file, orphan, tsconfig-perimeter, and dependency-class facts
re-measured from the tree. NOT RE-MEASURED: expo-doctor (network-gated,
reason at finding #19). No production code was modified.*

---

## Delta as of `ca6f028` (2026-07-08, Batch 7 / T6-54 — measured, not carried forward)

Baselines re-measured live after Batches 3–5c (`4012fa9..ca6f028`, 30+ commits).
Historical rev-2/rev-3 figures above are untouched — they were true when measured.

| Baseline | Rev 3 (2026-07-04) | Measured 2026-07-08 |
|---|---|---|
| Lint | 10 warnings | **8** (0 errors, 8 warnings) — re-run live; dropped at `3bf0f14` (N1-JS dead-plumbing removal) |
| Tests | 54 / 53 / 1 | **58 files / 57 green / 1 parked red** (phaseDetectionDTL, unchanged profile 9/7) — re-run live; +4 suites since rev 3 |
| God-files (>600 non-test, rev-3 scope: app/ lib/ packages/, excludes scripts/) | 6 | **8** — `wc -l` census: phaseDetectionFaceOn 1224 (−12), **outbox.ts 1067** (was 998; G6/G9/G8 + T4-96 drain work), settings 836 (=), analysisPipeline 831 (+17), **record.tsx 716** (was 698; T2-91 error path), phaseDetectionShared 673 (=), plus two NEW 600-crossers: `app/grip/capture.tsx` **614** and `app/analysis/result.tsx` **604** (crept back over the line since its 525-line decomposition) |
| Orphans | 4 | **2 of the listed 4 deleted** [`01194c2`]: GripHistoryRow.tsx + VisualCoachCard.tsx gone (their rows in the Axis-1(d) import census above are likewise historical). The 2 hand adapters remain from the listed set. **Full census NOT re-measured this pass** (needs a knip re-run) |
| Cycles | 7 | **NOT re-measured this pass** (needs a madge re-run); no known structural change to the 7 recorded |

Findings-ledger movement since rev 3:

- **#12 (supabase-js in devDependencies): RESOLVED** [`5d89769` / T2-19] — moved to `dependencies`.
- #14 (`category_scores` write-only), #15 (`reliability.impact` zero consumers — ticketed T7-86), #20 (vanished-feature effects) — re-verified still true during the Batch 5c/7 sweeps.

*Delta provenance: lint, full test suite, and the god-file census re-run live 2026-07-08; orphan statement derived from the `01194c2` deletion (grep-verified zero references at delete time). NOT re-measured: knip orphan census, madge cycles, expo-doctor. No production code modified in this pass (docs-only batch).*
