# Phase 0 Audit — iOS CNG Refactor

Source plan: `~/.claude/plans/honeyswing-ios-cng-tidy-ladybug.md`
Branch: `clean-release`
Date: 2026-05-19

## Pre-flight reads (recorded)

- `CFBundleShortVersionString` = `1.9.9` — matches `app.json` `version: "1.9.9"` ✓
- `CFBundleVersion` = `49` — needs `app.json ios.buildNumber: "49"` for local-build parity
- `DEVELOPMENT_TEAM` present in pbxproj — operator pastes real value at Phase 2 as `app.json ios.appleTeamId`

## diff-vs-HEAD verification

Every audited file matches `HEAD`. No silent local mutations to worry about during the refactor.

```
CLEAN  ios/honeyswing/Info.plist
CLEAN  ios/honeyswing/AppDelegate.swift
CLEAN  ios/honeyswing/honeyswing.entitlements
CLEAN  ios/honeyswing/Supporting/Expo.plist
CLEAN  ios/honeyswing.xcodeproj/xcshareddata/xcschemes/honeyswing.xcscheme
CLEAN  ios/honeyswing/honeyswing-Bridging-Header.h
CLEAN  ios/Podfile
CLEAN  ios/Podfile.properties.json
CLEAN  ios/honeyswing.xcodeproj/project.pbxproj
```

## CI / build-pipeline audit

- `.github/workflows/` — **does not exist**. No GitHub Actions reading `ios/**`.
- `eas.json` exists. `grep -rn "ios/"` against it returns no hits — no direct path-string references to tracked `ios/` files.
- `eas.json` contains: `appVersionSource: "remote"` and `build.production.ios.autoIncrement: true`. **Finding:** EAS Build manages `ios.buildNumber` remotely on cloud production builds; the `app.json ios.buildNumber: "49"` we set in Phase 2 controls local-build parity (matches current Info.plist) and seeds prebuild output. EAS production builds will continue to auto-increment from its own remote counter as before — no behavior change for cloud build pipeline.
- `ios/Podfile` `post_install` hook is the standard `react_native_post_install(installer, ..., :mac_catalyst_enabled => false, :ccache_enabled => ...)` — no Honey-custom logic. Regenerated identically by Expo prebuild.

**Conclusion: no CI surface direct-reads `ios/` tracked content. Safe to untrack.**

## Resolutions for previously UNVERIFIED audit rows

### Bridging Header → classification (a) confirmed
- `ios/honeyswing/honeyswing-Bridging-Header.h` contents: 3 comment-only lines, no `#import`.
- Cross-import check: `HoneyVisionCameraPosePlugin.m` calls Swift class `HoneyVisionCameraPosePlugin` via `VISION_EXPORT_SWIFT_FRAME_PROCESSOR` macro and direct ObjC bracket syntax (`[HoneyVisionCameraPosePlugin classifyGripFramesWithTimestamps:...]`). These rely on the auto-generated `<Module>-Swift.h` header (emitted by Xcode at build time), NOT on any `#import` in the bridging header.
- Result: bridging header regenerated as empty stub on every prebuild — no action needed.

### MediaPipeTasksVision pod provenance → classification (b), resolution: `extraPods`
- Probe 1: `grep -rln "MediaPipeTasksVision" package.json node_modules/*/package.json node_modules/*/expo-module.config.json node_modules/*/*.podspec` → **no matches**. Not autolinked from any JS dep.
- Probe 2: `grep -rn "extraPods\|MediaPipeTasksVision" app.json plugins/` → **no matches**. No existing extraPods config and no existing plugin re-injection.
- Probe 3: `node_modules/expo-build-properties/build/pluginConfig.d.ts:319` exposes `extraPods?: ExtraIosPodDependency[]` on `PluginConfigTypeIos`. Shape (line 362): `{ name: string; version?: string; configurations?; modular_headers?; source?; path?; podspec?; testspecs?; git?; branch?; tag?; commit? }`.
- Resolution: Phase 2 Step 4 adds `{ name: "MediaPipeTasksVision", version: "~> 0.10.9" }` to `expo-build-properties.ios.extraPods`. Phase 3 Block 7 grep validates Podfile re-emission. Phase 5 lockfile delta confirms no syntactic drift from current manual entry.

## Classification key

- **(a) Regenerated by Expo CNG** — no action; will be re-emitted on prebuild.
- **(b) Customization** — must be expressed via app.json / plugin / extraPods or it will be lost.
- **(c) Cannot be expressed declaratively** — needs a `withXcodeProject` mutation in `withHoneyNative.js`.

## Audit table

| File / Setting | Non-default content | Class | Plan |
|---|---|---|---|
| Info.plist | NSCameraUsageDescription | (a) | Already in `app.json ios.infoPlist` |
| Info.plist | NSLocationWhenInUseUsageDescription | (a) | Already in `app.json ios.infoPlist` |
| Info.plist | NSMotionUsageDescription | (b) | Add `app.json ios.infoPlist.NSMotionUsageDescription` |
| Info.plist | CADisableMinimumFrameDurationOnPhone = true | (b) | Add `app.json ios.infoPlist.CADisableMinimumFrameDurationOnPhone: true` |
| Info.plist | CFBundleVersion = `49` | (b) | Set `app.json ios.buildNumber: "49"`. EAS auto-increments on cloud production builds (per eas.json) — local parity only. |
| Info.plist | CFBundleShortVersionString = `1.9.9` | (a) | Matches `app.json version: "1.9.9"` ✓ |
| Info.plist | CFBundleURLTypes (`honeyswingv2`, `com.honeyswing.honeyswing-v2`, `exp+honeyswing-v2`) | (a) | Regenerated by Expo from `scheme` + `bundleIdentifier`. Phase 3 Block 7 must grep all 3; if any missing, escalate to (b) and add `ios.infoPlist.CFBundleURLTypes` override BEFORE Phase 4 |
| Info.plist | RCTNewArchEnabled = true | (a) | `app.json newArchEnabled: true` |
| Info.plist | NSUserActivityTypes = `$(PRODUCT_BUNDLE_IDENTIFIER).expo.index_route` | (a) | Regenerated from `ios.associatedDomains`. Phase 3 must grep `expo.index_route` |
| Info.plist | NSAppTransportSecurity (NSAllowsLocalNetworking) | (a) | Expo CNG default |
| Info.plist | UISupportedInterfaceOrientations incl. LandscapeLeft + LandscapeRight | (a) | Phase 3 must grep `UIInterfaceOrientationLandscapeLeft`; if absent, add `ios.infoPlist.UISupportedInterfaceOrientations` override BEFORE Phase 4 |
| Info.plist | UILaunchStoryboardName = SplashScreen | (a) | `expo-splash-screen` plugin emits |
| Info.plist | LSMinimumSystemVersion = 12.0 | (a) | Expo CNG default |
| Info.plist | UIRequiredDeviceCapabilities arm64 | (a) | Expo CNG default |
| Info.plist | UIRequiresFullScreen = false | (a) | Expo CNG default |
| Info.plist | UIStatusBarStyle = UIStatusBarStyleDefault | (a) | Expo CNG default |
| Info.plist | UIUserInterfaceStyle = Automatic | (a) | `app.json userInterfaceStyle: "automatic"` |
| Info.plist | UIViewControllerBasedStatusBarAppearance = false | (a) | Expo CNG default |
| honeyswing.entitlements | com.apple.developer.associated-domains = `applinks:honeyswing.com` | (a) | `app.json ios.associatedDomains` |
| SplashScreen.storyboard | Default expo-splash-screen output | (a) | Driven by `expo-splash-screen` plugin |
| Supporting/Expo.plist | EXUpdates* keys | (a) | `app.json updates.url` + `runtimeVersion` |
| xcscheme | Default (`honeyswing.xcscheme`, BlueprintId `13B07F861A680F5B00A75B9A`) | (a) | Regenerated |
| AppDelegate.swift | Vanilla Expo CNG | (a) | Regenerated |
| honeyswing-Bridging-Header.h | Empty stub (3 comment lines, no `#import`) — verified | (a) | Regenerated as empty stub |
| Podfile | `pod 'MediaPipeTasksVision', '~> 0.10.9'` — verified manual-only, not autolinked | (b) | `expo-build-properties.ios.extraPods: [{ name: "MediaPipeTasksVision", version: "~> 0.10.9" }]` |
| Podfile | `post_install` hook | (a) | Standard `react_native_post_install(...)` — no Honey-custom logic |
| Podfile.properties.json | `ios.deploymentTarget = "16.0"` | (a) | `expo-build-properties.ios.deploymentTarget` |
| Podfile.properties.json | `expo.jsEngine: "hermes"`, `EX_DEV_CLIENT_NETWORK_INSPECTOR`, `apple.privacyManifestAggregationEnabled`, `newArchEnabled`, `ios.forceStaticLinking` | (a) | Expo prebuild defaults |
| pbxproj | `CLANG_ALLOW_NON_MODULAR_INCLUDES_IN_FRAMEWORK_MODULES = YES` (Debug + Release, count = 2) | (c) | `withHoneyNative` sets via `project.updateBuildProperty` on Debug and Release |
| pbxproj | `DEVELOPMENT_TEAM` (Debug + Release) | (b) | `app.json ios.appleTeamId: "<APPLE_TEAM_ID>"` |
| pbxproj | `CODE_SIGN_STYLE = Automatic` | (a) | Expo prebuild default |
| pbxproj | `OTHER_LDFLAGS`, `OTHER_SWIFT_FLAGS`, `LIBRARY_SEARCH_PATHS`, `SWIFT_OBJC_BRIDGING_HEADER`, `GCC_PREPROCESSOR_DEFINITIONS` | (a) | Expo CNG defaults — confirm idempotent in Phase 3 |
| pbxproj | 7 Honey sources in PBXSourcesBuildPhase | (c) | `withHoneyNative` adds via `addBuildSourceFileToGroup` |
| pbxproj | 2 `.task` files in PBXResourcesBuildPhase | (c) | `withHoneyNative` adds via `addResourceFileToGroup` |
| pbxproj | Vision.framework link | (c) | **MISSING TODAY** — `withHoneyNative` adds via `IOSConfig.XcodeUtils.addFramework` |
| pbxproj | ARKit.framework link (currently linked) | (c) | `withHoneyNative` adds via `IOSConfig.XcodeUtils.addFramework` |
| eas.json | `appVersionSource: "remote"`, `production.ios.autoIncrement: true` | (a) | EAS-managed; no PR-side change |
| `.github/workflows/` | does not exist | n/a | No CI to update |

## STOP gate — Phase 0

- [x] `docs/PHASE_0_AUDIT.md` written
- [x] Every row classified (a) / (b)+plan / (c)+justification
- [x] Both previously UNVERIFIED rows resolved (Bridging Header → (a); MediaPipeTasksVision → (b) via extraPods)
- [x] CI audit recorded — no consumer surface direct-reads `ios/`
- [x] All audited files diff-clean vs HEAD
- [ ] **Operator approval to proceed to Phase 1**

(b) items to be applied in Phase 2 Step 4 as a single atomic `app.json` edit:
1. `ios.infoPlist.NSMotionUsageDescription`
2. `ios.infoPlist.CADisableMinimumFrameDurationOnPhone: true`
3. `ios.buildNumber: "49"`
4. `ios.appleTeamId: "<APPLE_TEAM_ID>"` (operator pastes real value)
5. `expo-build-properties` → `ios.extraPods: [{ name: "MediaPipeTasksVision", version: "~> 0.10.9" }]`
